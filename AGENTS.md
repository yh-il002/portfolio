# リポジトリ指示

## コマンド

- Install: `npm install`
- Dev: `npm run dev`
- Format: `Not configured`
- Lint: `Not configured`
- Typecheck: `Not configured`
- Unit test: `npm run test:run`
- Integration test: `Not configured`
- E2E: `Not configured`
- Build: `npm run build`

未設定のものは `Not configured` と記載している。存在しないコマンドを実行しない。

## 必須チェック

次のチェックがすべて通るまで、完了報告をしない。

- `npm run test:run`
- `npm run build`

失敗したチェックは、コマンド・終了コード・原因・対処を記録する。
実行しなかったチェックは、その理由を明示する。

## 変更方針

- **Codex がこのリポジトリの主計画・実装担当**。実装は Codex 側で行う。
- 承認された計画の範囲だけを変更する。
- 既存のコンポーネント、ユーティリティ、スタイル、設計トークンを再利用してから、新規追加を検討する。
- 依存パッケージの追加は事前に承認を得る。
- 生成物ファイルは、タスクが要求しない限り変更しない。
- 無関係なリファクタリングをしない。
- 振る舞いが変わる場合はテストを追加または更新する。
- チェックが失敗している状態で完了と報告しない。
- 必須チェックがすべて通ってから、Claude Code の外部レビューへ進む。
- Claude Code の指摘は入力であり、自動適用の対象ではない。要件と照らして採否を判断し、
  `resolution.md` に Accepted / Rejected / Deferred を記録する。

## フロントエンド要件

- セマンティック HTML とキーボード操作性を維持する。
- 該当する場合、loading / empty / error / disabled の各状態を確認する。
- プロジェクトが対応するブレークポイントでレスポンシブ挙動を確認する。
- 既存のコンポーネントと設計トークンを再利用してから、新規作成を検討する。

## レビュー観点

- 要件充足
- 実行時の挙動
- 状態とデータフロー
- 回帰リスク
- テストの十分さ
- アクセシビリティ（セマンティック HTML、キーボード操作、フォーカス、ARIA、状態変化）
- パフォーマンス（不要な再レンダリング、同期処理、バンドルサイズ。計測可能な根拠を伴うこと）

## 引き継ぎ

タスクの成果物は `docs/tasks/<task-slug>/` に置く。
ツール間の引き継ぎは、このディレクトリのファイルと `git diff` だけで完結させる。
会話履歴の共有を前提にしない。

`CLAUDE.md` に書かれた共通コマンドは、このファイルの `## コマンド` と一致させる。
片方だけを更新しない。
